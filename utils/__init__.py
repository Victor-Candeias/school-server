# Import the Utils class from the utils module
# The Utils class provides utility methods for encryption, decryption, password validation, and more
from utils.utils import Utils

# Create a singleton instance of the Utils class
# This instance, named 'utilities', can be accessed globally throughout the application
utilities = Utils()
